# ═══════════════════════════════════════════════════════════
#  SOLURIS — Variables d'environnement
# ═══════════════════════════════════════════════════════════
#  Copier ce fichier : cp .env.example .env
#  Puis remplir les valeurs
#
#  Sur Railway : ces variables se configurent dans
#  Dashboard → Service → Variables
# ═══════════════════════════════════════════════════════════

# ── BASE DE DONNÉES ──────────────────────────────────────
# Sur Railway : AUTO-INJECTÉE par le plugin PostgreSQL
# En local : installer PostgreSQL + pgvector
DATABASE_URL=postgresql://postgres:password@localhost:5432/soluris

# ── AUTHENTIFICATION ─────────────────────────────────────
# Générer avec : openssl rand -hex 32
JWT_SECRET=CHANGE_MOI_avec_une_longue_chaine_aleatoire

# ── IA — ANTHROPIC (obligatoire) ─────────────────────────
ANTHROPIC_API_KEY=sk-ant-api03-ta-cle-ici
# Modèle utilisé (optionnel, défaut = claude-sonnet-4-20250514)
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# ── EMBEDDINGS RAG (recommandé) ──────────────────────────
# Choisir UN provider : cohere (recommandé) ou openai
EMBEDDING_PROVIDER=cohere
COHERE_API_KEY=co-ta-cle-ici
# OU :
# EMBEDDING_PROVIDER=openai
# OPENAI_API_KEY=sk-ta-cle-ici

# ── ENVIRONNEMENT ────────────────────────────────────────
NODE_ENV=development
# Sur Railway : mettre "production"
